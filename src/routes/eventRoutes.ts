import { Router } from "express";
// ...importar controladores...

const router = Router();

// Definir rutas de eventos
router.get("/" /* controlador */);
router.post("/" /* controlador */);
// ...otras rutas...

export default router;
